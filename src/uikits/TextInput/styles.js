import {StyleSheet} from 'react-native';

import Color from '../../styles/color';

const styles = StyleSheet.create({
  main: {
    paddingVertical: 4,
    borderRadius: 24,
    paddingHorizontal: 24,
    borderWidth: 2,
    borderColor: Color.WHISPER,
    backgroundColor: Color.WHITE,
  },
});

export default styles;
